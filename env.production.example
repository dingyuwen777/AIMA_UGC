# Internal V1 / 服务器与 Production Compose 配置模板。
#
# 公司 Linux 服务器 / Production Runtime：
#   cp env.production.example env.production
#   chmod 0600 env.production
#   编辑 env.production 后运行：
#   docker compose --env-file env.production up -d --build --wait
#
# 本地源码开发与本地 Docker Compose 统一使用 env.local，不使用本文件。
# 正式离线 Release 在服务器上同样从本模板创建 env.production，并使用 canonical compose.yaml。
#
# 真实 env.production 包含 TikHub/LLM API Key，属于敏感文件：
# - 已由 .gitignore 忽略；
# - 不要提交 Git、打入 Release/镜像、粘贴到日志或工单；
# - PostgreSQL 密码和 Cursor signing keys 不在这里配置，由 AIMA 首次启动自动生成并持久保存。
#
# 本文件中的环境变量都是容器启动输入，不是热更新配置。修改 env.production 后需要通过上述
# Compose 命令重新创建/启动受影响服务。某些业务任务会在创建时冻结运行事实（下文单独说明）；
# 修改 env 不会反向改写已经冻结的 Campaign/Analysis Run 事实。
# 模板中的数值是当前安全默认值/背压起点，不代表服务器、数据库或外部 Provider 的最大能力。

# ------------------------------------------------------------
# Production Runtime：镜像、监听与容器网络
# ------------------------------------------------------------
AIMA_IMAGE_TAG=internal-v1a

# 只控制前端端口在服务器哪一个宿主地址上暴露；必须改成目标服务器真实可绑定的地址。
# 它不改变 API 容器内部端口，也不控制 Worker/数据库/LLM 并发。
AIMA_HTTP_BIND_IP=192.168.13.29
AIMA_HTTP_PORT=8080

# Docker Compose 内部 app bridge 网络。
# 默认网段为 10.1.1.0/24，网关为 10.1.1.1；容器继续使用 service name 通信，不依赖固定容器 IP。
# 这里只定义 Docker IPAM，不是业务吞吐/数据容量上限。
# 部署前必须确认该网段不与宿主 LAN、VPN 或其他 Docker network 冲突。
# 如需覆盖，AIMA_DOCKER_SUBNET 与 AIMA_DOCKER_GATEWAY 必须成对修改并保持 gateway 位于 subnet 内。
AIMA_DOCKER_SUBNET=10.1.1.0/24
AIMA_DOCKER_GATEWAY=10.1.1.1

# Dockerfile / Compose 中的基础镜像固定为官方 Docker Hub canonical reference：
# Python     python:3.14.7-slim-trixie
# Node       node:24.19.0-bookworm-slim
# Nginx      nginx:1.30.4-alpine3.24
# PostgreSQL postgres:18.4
# uv 0.12.3 在 Python builder 中由 PyPI 安装，不使用独立 GHCR uv 镜像。
# Docker Hub 下载加速由首次环境初始化写入宿主 Docker Engine registry-mirrors；
# env.production 不负责改写容器镜像名称。
#
# Debian / PyPI / npm 是构建期包下载源。默认使用国内镜像，可按机器显式覆盖；
# 这些字段只改变构建下载路径，不改变依赖版本、lockfile、镜像 tag 或业务 Runtime Contract。
AIMA_BUILD_DEBIAN_MIRROR=https://mirrors.aliyun.com/debian
AIMA_BUILD_DEBIAN_SECURITY_MIRROR=https://mirrors.aliyun.com/debian-security
AIMA_BUILD_PYPI_INDEX=https://pypi.tuna.tsinghua.edu.cn/simple
AIMA_BUILD_NPM_REGISTRY=https://registry.npmmirror.com

# Docker Runtime 的宿主持久化根目录。
# 公司服务器推荐保持 /data/AIMA_UGC，使数据库、Artifact、日志和内部 Secret 与 Release 生命周期解耦。
AIMA_HOST_ROOT=/data/AIMA_UGC

# ------------------------------------------------------------
# 统一数据导入：路径、安全护栏与 Job 背压
# ------------------------------------------------------------
# 管理员预先放置历史 Excel 的只读目录。
# HOST_ROOT 是服务器宿主 source；ROOT 是 API/Worker 容器内看到的 bind mount target。
# Compose 使用同一个 ROOT 同时配置 backend environment 与 mount target，避免两者漂移。
AIMA_HISTORICAL_IMPORT_HOST_ROOT=/data/AIMA_UGC/aima-historical-input
AIMA_HISTORICAL_IMPORT_ROOT=/data/AIMA_UGC/aima-historical-input

# Chunk Rows：每个新 Data Import Campaign 创建时冻结到 profile_snapshot 的 Chunk 粒度。
# 当前 Settings 接受 100—2000；1000 是默认事务/恢复粒度，不是总导入行数上限。
# 修改 env 并重建服务后只影响之后创建的 Campaign，不能重切已经冻结的 Campaign Chunk。
AIMA_HISTORICAL_CHUNK_ROWS=1000

# Server Path Discover 安全护栏：限制一次递归发现允许纳入的 XLSX 文件数和目录深度。
# 当前 Settings 分别接受 1—100000 与 1—32；实际目录超过配置阈值时 Discover 会失败关闭，
# 以避免误扫大型共享盘或异常目录树。它们不是导入完成后的数据库容量限制。
AIMA_HISTORICAL_MAX_SCAN_FILES=10000
AIMA_HISTORICAL_MAX_DIRECTORY_DEPTH=8

# Historical Job 背压窗口：限制同一 Campaign 同时处于未完成调度状态的 Snapshot/Chunk Job 数。
# 当前 Settings 接受 1—16。它控制 Job 表积压和资源压力，不承诺相同数量的物理 Worker 并发；
# 当前 canonical compose.yaml 只有一个 Worker 服务，单纯把本值调到最大不会线性提高导入吞吐。
# 修改 env 并重新创建 Worker/API 后，后续调度读取新的 Runtime 值；已冻结 Chunk 本身不改变。
AIMA_HISTORICAL_MAX_IN_FLIGHT_JOBS=2

# ------------------------------------------------------------
# Analysis Run：Shard Job 背压
# ------------------------------------------------------------
# 这里只配置 Analysis Shard 的 queued/running Job 窗口，当前 Settings 接受 1—16。
# 它不是 LLM 请求并发，也不是模型吞吐上限：一个 Shard 内的实际并发来自该 Run 冻结的
# Provider max_concurrency；可选 max_rps 也随 Provider Snapshot 冻结。
# Shard Size 由 Provider max_concurrency / max_rps 自动推导并在创建 Run 时冻结，不在 env 配置。
# 修改本值并重新创建 Worker 后只改变后续 Shard 调度窗口，不会改写已有 Run 冻结的 Provider/Shard Size。
AIMA_ANALYSIS_RUN_MAX_IN_FLIGHT_JOBS=2

# ------------------------------------------------------------
# PostgreSQL 与日志
# ------------------------------------------------------------
AIMA_DB_NAME=aima_ugc
AIMA_DB_USER=aima_ugc

# 只限制建立 PostgreSQL 连接时的等待时间，不是 SQL 查询、事务或 Job 执行超时。
AIMA_DB_CONNECT_TIMEOUT_SECONDS=5

# 日志级别与文件轮转/保留策略；限制的是日志文件资源，不限制业务处理能力。
AIMA_LOG_LEVEL=INFO
AIMA_LOG_MAX_BYTES=20971520
AIMA_LOG_BACKUP_COUNT=10
AIMA_LOG_COMPRESS=true

# ------------------------------------------------------------
# TikHub（可选能力，模板默认启用）
# ------------------------------------------------------------
# 模板默认 AIMA_TIKHUB_ENABLED=true，并保留标准 TikHub Base URL。
# 在数据库尚未接管稳定 TikHub Provider 的新环境中，首次 Compose 启动前必须填写 AIMA_TIKHUB_API_KEY；
# Key 为空会在 configure 阶段 fail closed，不会把“开启但没有凭据”静默视为可用。
# Compose 会把 API Key 转成 Secret 文件，不作为业务容器普通环境变量传入，也不会把真实 Key 写入 PostgreSQL。
# 一旦稳定 TikHub Provider 已存在于数据库，后续启停和非敏感配置由管理员控制面维护；
# 修改 env 不会在启动时覆盖数据库中的现有 Provider 配置。
AIMA_TIKHUB_ENABLED=true
AIMA_TIKHUB_BASE_URL=https://api.tikhub.io
AIMA_TIKHUB_API_KEY=

# ------------------------------------------------------------
# AI / LLM（可选能力，模板默认启用）
# ------------------------------------------------------------
# Base URL / Provider Name / Model 默认作为 Runtime 启动输入存在；模板仍不会保存真实 API Key。
# 在数据库尚未存在任何 LLM Provider 的新环境中，首次 Compose 启动前必须填写 AIMA_LLM_API_KEY；
# Key 为空会在 configure 阶段 fail closed，不会把半配置静默解释为可用。
# Compose 会把 API Key 转成 Secret 文件，不作为业务容器普通环境变量传入。
# env LLM 只在数据库尚未存在任何 LLM Provider 时承担 bootstrap/兼容 fallback；
# 一旦管理员创建过任意 LLM Provider，数据库就是唯一运行时事实源，禁用/无默认项也不会回退 env。
# 正式生产吞吐容量请在管理员 Provider 中配置 max_concurrency / max_rps；本模板不再提供平行的
# AIMA_LLM_MAX_CONNECTIONS 调优入口，避免 env 与数据库形成两套生产容量事实。
AIMA_LLM_BASE_URL=https://api.deepseek.com
AIMA_LLM_PROVIDER_NAME=api.deepseek.com
AIMA_LLM_MODEL=deepseek-v4-pro
AIMA_LLM_API_KEY=
