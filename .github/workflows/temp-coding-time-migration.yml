name: Temporary Coding Time Migration

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - name: Disabled
        run: echo "一次性北京时间迁移已停用"
