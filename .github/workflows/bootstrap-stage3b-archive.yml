name: Bootstrap Stage 3B archive
on: push
permissions:
  contents: write
jobs:
  archive:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          ref: docs/archive-stage3b-canonical-v1
      - run: echo ready
