name: Runtime Config Hash Finalize (Temporary)

on:
  push:
    branches:
      - feat/runtime-config-control-plane
    paths:
      - scripts/dev/runtime_config_hash_finalize.py
      - .github/workflows/runtime-config-hash-finalize.yml
      - tests/unit/system/test_runtime_provider_config.py

permissions:
  contents: write

concurrency:
  group: runtime-config-hash-finalize
  cancel-in-progress: false

jobs:
  finalize:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout feature branch
        uses: actions/checkout@v7.0.1
        with:
          ref: feat/runtime-config-control-plane
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v6.2.0
        with:
          python-version: '3.14.7'

      - name: Install frozen Python environment
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --disable-pip-version-check 'uv==0.12.3'
          uv sync --locked

      - name: Apply and verify Analysis runtime hash fix
        shell: bash
        run: |
          set -euo pipefail
          uv run python scripts/dev/runtime_config_hash_finalize.py
          uv run ruff format --check backend tests \
            migrations/versions/20260814_0005_stage5d_provider_dispatch.py \
            migrations/versions/20260815_0013_stage7_plan_occurrence_run_snapshot.py \
            migrations/versions/20260815_0014_stage7_latest_only_scheduler_policy.py
          uv run ruff check backend tests \
            migrations/versions/20260814_0005_stage5d_provider_dispatch.py \
            migrations/versions/20260815_0013_stage7_plan_occurrence_run_snapshot.py \
            migrations/versions/20260815_0014_stage7_latest_only_scheduler_policy.py

      - name: Commit fix and remove temporary tooling
        shell: bash
        run: |
          set -euo pipefail
          rm -f scripts/dev/runtime_config_hash_finalize.py .github/workflows/runtime-config-hash-finalize.yml
          git config user.name 'aima-runtime-finalizer'
          git config user.email 'actions@users.noreply.github.com'
          git add -A
          git diff --cached --check
          git commit -m 'fix(analysis): 配置哈希纳入冻结 Provider 快照'
          git push origin HEAD:feat/runtime-config-control-plane
